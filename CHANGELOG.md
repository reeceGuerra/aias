# Changelog

Rho AIAS uses a lightweight architecture versioning scheme:

- **Major**: fundamental change in structure, philosophy, or artifact relationships
- **Minor**: new mode, command, skill, contract update, or behavioral refinement

## Current Version

The current version of the architecture is **v8.7**.

## Versioning Scheme

| Level | When to bump | Example |
|---|---|---|
| **Major** (v4 -> v5) | Fundamental change in structure, philosophy, or artifact relationships | Redesign two-message pattern, introduce autonomous sub-agents, break backward compatibility |
| **Minor** (v4.6 -> v4.7) | New mode, command, skill, contract update, or behavioral refinement | Add `@devops`, create `/blueprint`, trim `planning.mdc`, add `CONTEXT ENRICHMENT` to modes |

## Release History

| Version | Milestone |
|---|---|
| v4.0 | Skills architecture, `@delivery` + `/charter`, MCP extraction from modes into skills (`atlassian-mcp`, `figma-mcp`, `github-mcp`) |
| v4.1 | Figma Design Specification in planning, `planning.mdc` trimming |
| v4.2 | `@impl` renamed to `@dev`, `/implement` command created |
| v4.3 | `xcode-mcp` skill, `CONTEXT ENRICHMENT` added to modes |
| v4.4 | `/blueprint` command, `planning.mdc` decoupled from data collection, `COMMANDS AND SKILLS` in base rules |
| v4.5 | `DESIGN SYSTEM MAPPING` (RDS tokens) in iOS modes, SwiftUI preview enforcement in `@dev` |
| v4.6 | `@devops` transversal mode, CI/CD pipeline flow in workflows |
| v4.7 | `/trace` command for log instrumentation plans (`@qa`, `@debug`) |
| v4.8 | `/run` and `/test` commands, `/spm` refactored (consistent hybrid invocation), `@tooling` mode |
| v4.9 | `/enrich` command, `@explore` with product analysis frameworks, `@dev` enhanced (CODE PATTERNS, DEVELOPMENT WORKFLOW, xcode-mcp safeguard), `continuous-improvement.mdc` transversal rule, `model`/`color` on all modes, language standards expanded in base rules, `/fill-plan` Jira ID naming, `/commit` documentation awareness |
| v4.10 | `/explain` command, `/implement` feedback handling (inline/defer/systemic), `/blueprint` comprehension gate + checkpoint + `--fast` flag, `/fill-plan` preview gate, `/commit` improved (branch safeguard, optional body, final summary) |
| v4.11 | `technical-writing` skill (non-MCP, 6 patterns: Problem Framing, Acceptance Criteria, Root Cause, Risk, Increment Goals, Conciseness), referenced by 9 artifact-producing commands |
| v4.12 | `incremental-decomposition` skill (non-MCP, 6 rules: Self-Containment, Boundary Criteria, Ordering, Sizing, Naming/Goals, Improvement Margin), referenced by 5 commands; scattered increment rules consolidated |
| v4.13 | `jira-field-mapping.md` in atlassian-mcp skill — traceability explore -> enrich -> Jira fields, field catalogs with IDs, format rules, push behavior; `/enrich` Phase 5 now writes to specific fields (`customfield_10036`, `customfield_10062`, `priority`, `components`); `/report` and `/brief` reference mapping when publishing |
| v5.0 | **Artifact Directory Architecture** — Centralized task artifacts under `~/.cursor/plans/<TASK_ID>/`, `rho-aias` system skill (7-phase loading protocol, closed artifact catalog, status tracking), `status.md` with 6 states, progressive Confluence publishing (Phase 5c), Jira sync (Phase 6) with `tracker-status-mapping.md`, `@product` mode replacing `@explore`, `/blueprint` absorbs `/fill-plan`, `/publish` closure command, `/copyedit` generalized, Plan Delta in `/pr`, `readme-artifact.md` contract, multi-tool portability (`.claude/`, `.windsurf/` placeholders), all modes updated with globs + rho-aias reference |
| v5.1 | **Workflow Profiles + Plan Classification** — `/assessment` command (`feasibility.assessment.md`), Plan Classification (A/B/C) assigned by `/blueprint` and escalatable by `/charter`, `delivery` workflow profile, artifact reference fields in Structured Prompt (`ISSUE:`, `FIX:`, `ASSESSMENT:`, `TRACE:`), "one mode per chat" rule formalized, all profile tables with Mode/Chat columns, bugfix flow with `/trace` (conditional) and `/assessment`, `/consolidate-plan` and `/commit` in all profiles, `/publish` decoupled from modes, DESIGN SYSTEM MAPPING delegation in `/blueprint`, catalog expanded to 12 artifacts |
| v5.1 (Phase 0) | **Phase 0 complete** — Brand foundation document created (`BRAND.md`). Defines mission, vision, values, origin story, audience, value proposition, differentiators, and tone. |
| v6.0 | **Phase 1 complete (BL-005)** — physical rebranding executed: `cursorconfig/` renamed to `aias/`, operational references migrated across contracts/docs/prompts/intents/workspaces, and runtime sync completed for `~/.cursor/commands` + `~/.cursor/skills`. |
| v6.1 | **Phase 4 complete (BL-011, BL-012, BL-013)** — Rule Abstraction: `readme-output-contract.md` contract created (including Build System Integration Fragments lifecycle), `readme-base-rule.md` extended with invariant/parametrizable taxonomy and category-driven mandate, `readme-stack-profile.md` extended with rule bindings (`binding.rule.base.*`, `binding.rule.output_contract.*`), canonical rule templates (`base-rule-template.md`, `output-contract-template.md`), output-contract fragment system (`output-contract-fragments/` + `TEMPLATE.md`), stack profiles extended with per-workspace rule bindings (shared + workspace-specific + fallback), `generate_modes.py` renamed to `generate_modes_and_rules.py` (mode + rule generation), Pre-flight Validation Protocol (G0-G4: infrastructure, profile discovery, mode bindings, rule bindings, fragment validation), provider hardcoding removed from 7 `base.mdc` ("Jira" -> "Tracker"), rule validation matrix and maintenance workflow documented. BL-S04 absorbed. Validated via idempotency check (26 files, 0 diffs) and iOS recreate-from-scratch test (20 iOS files, 0 diffs). |
| v6.1+ | **Phase 5 complete (BL-010)** — Tool Portability: multi-tool support for Cursor, Claude Code, Windsurf, GitHub Copilot, and Gemini via shortcut files (zero content duplication). `readme-tool-adapter.md` contract (shortcut format per tool, fidelity matrix, G5-G7 validation). `readme-project-context.md` contract (`RHOAIAS.md` as single source of truth for project context). `aias-config/rules/` and `aias-config/modes/` as canonical flat output directories. `aias/.commands/` (renamed from `aias/commands/`). Context shortcuts generated via CLI/command (no template files on disk). Generator extended with `--shortcuts` flag for multi-tool shortcut generation, pre-flight G5, post-flight G6-G7. Plans path abstracted to configurable `<resolved_plans_dir>` (default: `~/.cursor/plans/`). All contracts, commands, docs updated for tool-agnostic language. |
| v6.1+ | **BL-S10 — Generator Input Relocation**: Single profile + single fragment model (`<repo_root>/stack-profile.md`, `<repo_root>/stack-fragment.md`). Workspace-specific fragments eliminated from `output-contract-fragments/`. Codex added to multitooling (`.codex/commands/`, `.agents/skills/`, `codex.md`). `aias/skills/` renamed to `aias/.skills/` (consistency with `.rules/`, `.modes/`, `.commands/`). Skill shortcuts generated for Cursor (`.cursor/skills/`), Claude Code (`.claude/skills/`), and Codex (`.agents/skills/`). Claude Code Skills updated from None to Partial. G6 expanded for all tools. |
| v6.1+ | **BL-S08 — Customization UX**: Hybrid CLI + AI command system for artifact generation. `readme-agent.md` eliminated (superseded by `readme-project-context.md`). Canonical rule sources renamed (removed `-template` suffix). `output-contract-fragments/` fully absorbed into `readme-output-contract.md` § Fragment Structure Options. `mode-equivalence-matrix.md` eliminated. `aias_cli.py` created (4 subcommands: `init`, `new`, `generate`, `health`). `/aias` command (Type B) with smart suggestion protocol. `aias/docs/CLI.md` reference documentation. |
| v7.0 | **Phase 6 complete (BL-016)** — Packaging and Documentation: `aias/README.md` (framework front door with mission, vision, 7 layers, quick start, multi-tool portability), `aias/docs/ARCHITECTURE.md` (deep architecture overview), `aias/docs/CONFIGURATION.md` (configuration guide absorbing BL-014), `aias/CONTRIBUTING.md` (contribution guide), `aias/BRAND.md` trimmed (mission/vision/audience/value proposition extracted to README), Phase 6 impact sweep completed. Framework is fully agnostic, distributable, and open-source ready. |
| v7.1 | **Post-distribution hardening** — Phase 5c classification-gated (knowledge sync skips if classification is `null` or `A`, fires only for `B`/`C`; `/publish` bypasses gate), `continuous-improvement.mdc` moved to `.canonical/` as regenerable source, `binding.generation.tasks_dir` in `stack-profile.md` (replaces `plans_base` in `RHOAIAS.md`; `resolved_plans_dir` renamed to `resolved_tasks_dir` across framework), `binding.generation.tools` in `stack-profile.md` (tool-scoped generation + `aias health` scoping), Atlassian MCP v2 skill update (`spaceKey` -> `spaceId` runtime resolution, `contentFormat`/`responseContentFormat`, new tools), knowledge publishing anti-summarization rules (full Markdown content mandate), Confluence artifact title uniqueness (`<TASK_ID>: <artifact_filename>` convention with legacy fallback for `modified` artifacts). |
| v7.2 | **Execution + publishing hardening** — `/implement` adds a Verification Gate for iOS via `xcode-mcp` (build + full test suite after each increment, with failure handling gate), and Confluence hierarchy titles are hardened for repeating nodes (`<TECH>: <YEAR>` and `<TECH>: <YEAR>: <QUARTER>`) while keeping artifact titles task-scoped and preserving legacy fallback lookup for existing pages. |
| v7.3 | **Tracker RCA restructuring** — `/report` becomes the owner of bug RCA publication with field-first/comment-last behavior, Evidence Sufficiency gate, and support for structured RCA fields plus fallback comment when needed; `/enrich` gains classification shaping with a Comprehension gate and is explicitly removed from RCA ownership; `jira-field-mapping.md` is reorganized by issue type (`Story` + `Bug`) with RCA catalogs (`IDs + labels`) and runtime-metadata precedence documented for Jira provider maintenance. |
| v7.4 | **Review + handoff hardening** — `/peer-review` gains layered output with severity findings, VCS-ready review snippets, and a PR-level general review comment; new `/handoff` command adds operational cross-chat transfer snippets while preserving TASK_DIR artifacts as the durable handoff layer; `Type A` command contracts now allow read-only provider calls without permitting external mutation; review and handoff semantics are reconciled across contracts, `rho-aias`, architecture, workflows, quick start, and workspace command catalogs. |
| v7.5 | **Provider Config Externalization + Write Preflight** — Three project-specific config files (`jira-field-mapping.md`, `tracker-status-mapping.md`, `confluence-config.md`) migrate from `aias/.skills/atlassian-mcp/` to `aias-config/providers/<provider>/` (deprecated in skill, not deleted in v7.5); new contracts `readme-tracker-field-mapping.md` and `readme-knowledge-publishing-config.md`; `readme-provider-config.md` gains Referenced Document Directory, `resource_files` as dependency manifest, and new validation rules (`FIELD_MAPPING_RESOLVABLE`, `CONFIG_SOURCE_RESOLVABLE`, `RESOURCE_FILES_RESOLVABLE`); new `/aias configure-providers` AI command with MCP discovery and contractual fallback; `aias init` drops provider setup (7→6 steps); `aias health` detects `[LEGACY]` locations and AI agent offers interactive migration gate; `new --provider` generates `resource_files: []` for tracker/knowledge; `/enrich` gains mandatory mapping load + Phase 3b Field Write Plan with deterministic format resolution; `/report` gains mapping load in SERVICE RESOLUTION PSEUDOFLOW and format-resolved write plan in Gate: Tracker Publish; `atlassian-mcp/SKILL.md` adds PRE-WRITE RESOLUTION PROTOCOL and separation-of-concerns note; `jira-field-mapping.md` Format column refined from preference to deterministic value per field; 6 obsolete `intents/` and `prompts/` files cleaned up. |
| v7.6 | **Submodule Read-Only + `aias-config/` Unification** — `aias/` becomes strictly read-only (no generated or mutable files inside the submodule). All per-project content consolidated under `aias-config/`: `aias-config/rules/` (generated rules, ex `aias/.rules/`), `aias-config/modes/` (generated modes, ex `aias/.modes/`), `aias-config/providers/` (provider configs, ex `aias-providers/`), `aias-config/commands/` (custom project commands), `aias-config/skills/` (custom project skills). Generator and CLI updated with dual-source discovery for commands and skills (framework `aias/.commands/` + project `aias-config/commands/`). Tool adapter contract updated for split source locations. `aias health` gains legacy detection for `aias-providers/`, `aias/.rules/`, `aias/.modes/`, and legacy shortcut targets. `/aias health` AI migration extended with three scenarios (provider directory migration, rules/modes regeneration, shortcut re-targeting). Two-phase deprecation: v7.6 WARN/coexist, v8.0 removal. All 13 contracts, 10+ commands, 6 canonical mode templates, 5 doc files, and `AGENTS.md` updated with new paths. |
| v8.0 | **SDLC Refinement Restructure** — DoR/DoD ownership moves from `/blueprint` to `/enrich`, making refinement the canonical source of scope and criteria. `/blueprint` becomes strict (requires DoR/DoD, except bug flow with assessment). `/blueprint` reduced from 13 to 7 data categories (DoR/DoD categories removed). DoR Readiness check gate with blocker/non-blocking classification. Feature and bugfix DoR/DoD templates with "Out of scope" as new refinement dimension. Phase 5c (knowledge publishing) is now **unconditional** — always publishes regardless of plan classification A/B/C; classification is retained only for governance gates. `refinement_validated` flag in `status.md`. Amendment proposal mechanism for DoR/DoD changes discovered during planning (approved via `/validate-plan` Amendment gate with `apply_local`/`pause`/`reject`). `/validate-plan` shifts from content validation to alignment validation for DoR/DoD; no longer transitions tracker. `/consolidate-plan` gains Amendment gate for DoR/DoD artifacts (separate from Update Approval for technical artifacts). Tracker transitions realigned: `/enrich` owns `pending_dor → ready` (after publish), `/blueprint` owns `ready → in_progress`, `/validate-plan` and `/implement` no longer transition tracker. `/implement` no longer writes `status: in_progress` (already set by `/blueprint`). `/publish` reframed as reconciliation + closure (no longer classification-gated safety net). Classification gates relaxed when team refinement is validated. Separation of "what to test" (DoR Test criteria in `/enrich`) from "how to test" (Blueprint Cat 9 Testing). |
| v8.1 | **Skill v8.0 Alignment + Design Gaps** — Bug exception tracker transition: `/blueprint` bug exception now transitions `pending_dor` → `in_progress` directly (skipping `ready`) when DoR/DoD are generated via bug exception. Refactor profile: requires preceding `/enrich` step with new DoR refactor template (6 dimensions: technical debt metric, scope boundary, target state, regression risk, test criteria, out of scope). DoR spike template (lightweight, time-boxed: hypothesis, time-box, success criteria, out of scope). `spike` added as `work_type` in `/enrich`. Plan Classification table updated: publication is unconditional (Phase 5c) across all types; `/publish` framed as reconciliation + closure. Phase 6 ownership complete: `/enrich` owns `pending_dor → ready`, `/blueprint` owns `ready → in_progress`, `/blueprint` bug exception owns `pending_dor → in_progress`, `/pr` owns `in_progress → in_review`. Provider-level `tracker-status-mapping.md` corrected (BL-S15). `rho-aias` skill fully aligned with v8.0: `SKILL.md` (classification, ownership, v8.0 concepts), `reference.md` (profiles, step definitions, status.md format, ownership rules, state transitions), `examples.md` (all flow examples corrected — feature, bugfix, refactor, enrichment, artifact sync progression). |
| v8.2 | **Generator Hardening (BL-S16 + BL-S17)** — G6/G7 postflight now exits non-zero on failure (renamed WARNINGS → ERRORS). G7 threshold documentation aligned to 500 bytes (matching code and tool-adapter contract). Multi-workspace rule overwrite emits explicit warning (last-wins behavior documented). `binding.generation.mode_output_dir` deprecated (optional; legacy `aias/.modes` emits error). Transversal modes (`delivery.mdc`, `devops.mdc`) converted from verbatim copy to Handlebars templates with built-in defaults — all 9 modes are now template-based. `/aias health` gains shortcut integrity check (G6/G7 reused from generator). `aias.md` Scenario E for shortcut integrity failures. |
| v8.3 | **Contract Governance (BL-S18 + BL-S19)** — RFC-2119 keyword convention preamble added to all 12 contracts (referencing `readme-commands.md` § RFC-2119 Keyword Policy). Informal keywords normalized to uppercase (MUST, MUST NOT, SHOULD, MAY) across all contracts. New `readme-versioning-policy.md` (v1.0): Major.Minor versioning scheme, deprecation policy (minimum 1 major framework version coexistence), backward compatibility rules, current version registry for 14 contracts. Version numbers added to 5 contracts previously unversioned (all initialized at v1.0). `CONTRIBUTING.md` updated with contract versioning subsection and PR guidelines. Contract count: 13 → 14. |
| v8.4 | **Adopter Experience (BL-S20 + BL-S21 + BL-S22)** — **Terminology rename**: Command types renamed from "Type A/Type B" to **Advisory/Operative** (22 command headers + contract + architecture + CLI template). Plan Classification renamed from "A/B/C" to **Minor/Standard/Critical** (18 files: contracts, skills, commands, docs). `classification` field values in `status.md` changed from `A\|B\|C` to `minor\|standard\|critical`. "Type A Terminology" disambiguation section removed from `readme-commands.md` and `SKILL.md` (no longer needed — naming collision eliminated). **Progressive adoption**: new `PROGRESSIVE-ADOPTION.md` with 3-level adoption model (Minimum Viable → Team Workflow → Full Governance). `QUICKSTART.md`, `README.md`, `CONFIGURATION.md` updated with adoption references. **Meta-workspace dogfooding**: 4 provider config files created (`tracker-config.md`, `knowledge-config.md`, `design-config.md`, `vcs-config.md`) completing the service resolution chain. Historical CHANGELOG entries preserved; this entry documents the rename. |
| v8.5 | **Enrich Brief + TOC Confluence + Eliminate /brief** — Four converging changes across 32 files. **(1) TOC Confluence**: Every artifact published to Confluence receives a native TOC macro (ADF extension node) via post-publish injection (`injectTocIfMissing` algorithm in `confluence-config.md`); idempotent and non-blocking. Contract `readme-knowledge-publishing-config.md` gains optional TOC section. **(2) Enrich brief comment**: `/enrich` now posts an executive summary (Gap Summary, Problem Statement, User Flow, UI Spec, API/Data Contract, Dependencies, Out of Scope, User Journey, Acceptance Criteria) as a Jira comment via `addCommentToJiraIssue`. Brief Comment Preview gate (post/adjust/skip) replaces the default tracker write flow. Tracker field writes (Description, AC, Test Steps) become optional — only executed when user explicitly requests them. **(3) Remove tracker transition**: `/enrich` no longer transitions `pending_dor → ready`. That transition is now manual (team responsibility during refinement). `refinement_validated` semantics relaxed: `true` = brief comment posted + knowledge publish succeeded. `/blueprint` Phase 0 gains knowledge provider fallback for DoR/DoD (fetches from Confluence when not local). **(4) Eliminate /brief**: `brief.md` deleted, 21 commands (was 22). All references updated across contracts, skills, docs, provider configs, and examples. `reports/*.md` excluded as immutable historical artifacts. |
| v8.6 | **Legacy Skill Config Cleanup** — Deprecated config files removed from `aias/.skills/atlassian-mcp/`: `confluence-config.md`, `jira-field-mapping.md`, `tracker-status-mapping.md` (deprecated since v7.5, coexistence period expired in v8.0). "Legacy location" sections removed from contracts `readme-tracker-field-mapping.md`, `readme-knowledge-publishing-config.md`, `readme-tracker-status-mapping.md`. Legacy skill path validation removed from `readme-provider-config.md`. Scenario D (skill path migration) removed from `/aias health` migration assistance (A→E scenarios reduced to A→D+E renumbered). `LEGACY_PREFIXES` in CLI updated to remove `aias/.skills/` prefix. All `resource_files` paths must now resolve under `aias-config/providers/`. |
| v8.7 | **Enrich Flag Refactor (`--brief` / `--fields`)** — `/enrich` base behavior changed: without flags, only local analysis + knowledge publish (Confluence). Two additive flags introduced: `--brief` (posts enrichment brief as Jira comment; required for `refinement_validated: true`) and `--fields` (writes structured tracker fields — Description, AC, Test Steps). Gates are flag-conditional: Brief Comment Preview fires only with `--brief`; Tracker Write Preview fires only with `--fields`. Absence of flags is not a gate bypass (documented in `enrich.md` and `readme-commands.md`). `refinement_validated` semantics tightened: `true` only when `--brief` used + brief posted + knowledge publish succeeded; `false` without `--brief`. **v8.5 debt corrected**: `/enrich` removed from tracker transition list in `readme-commands.md` (was incorrectly listed since v8.5); `WORKFLOWS.md` "Ticket Enrichment Flow" rewritten to reflect flag-driven flow; `validate-plan.md` transition attribution to `/enrich` removed. 19 files updated across commands, contracts, skills, docs, and examples. **Known issue**: `implement.md` does not verify `refinement_validated` — pre-existing gap, not corrected in this release. |
